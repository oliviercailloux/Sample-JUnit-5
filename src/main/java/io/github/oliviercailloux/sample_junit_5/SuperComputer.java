package io.github.oliviercailloux.sample_junit_5;

public class SuperComputer {
	public int complicatedComputation(int parameter) {
		/** Assume we’re doing a very complicated computation here. */
		return parameter * 2;
	}
}
